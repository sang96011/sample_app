<% provide(:title, t("user.edit")) %>
<% provide(:button_text, t("user.save")) %>
<h1><%= t("user.update") %></h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render "form" %>
    <div class="gravatar_edit">
      <%= gravatar_for @user %>
      <%= link_to t("user.change"), "http://gravatar.com/emails", target: "_blank", rel: "noopener"  %>
    </div>
  </div>
</div>
